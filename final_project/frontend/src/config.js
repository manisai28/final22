// API configuration
export const API_URL = 'http://localhost:8000';

// App configuration
export const APP_NAME = 'Video SEO Analysis';

// Default settings
export const DEFAULT_SETTINGS = {
  darkMode: false,
  topKeywords: 10,
};

// Toast configuration
export const TOAST_CONFIG = {
  position: 'bottom-right',
  autoClose: 5000,
  hideProgressBar: false,
  closeOnClick: true,
  pauseOnHover: true,
  draggable: true,
};
